# 装机任务管理器（TaskDesk）需求文档

## 产品愿景

为新装 Windows 11 的个人用户提供一个无需复杂配置、打开即可使用的装机任务管理器。用户可以集中查看装机任务、资源库说明文件及相关资源，并在不修改系统和资源库源文件的前提下跟踪任务状态。

## 一、项目目的

- 使用者：新装 Windows 11 的个人用户，以及需要整理装机流程的维护人员。
- 要解决的问题：装机任务、说明文档和工具资源分散在文件夹中，缺少一个可直接打开、可跟踪状态且不要求管理员权限的统一入口。
- 一句话目标：新装 Windows 11 后无需复杂配置即可打开，以文件管理器式布局查看装机任务、说明文件与相关资源，并跟踪任务状态。

## 二、功能任务

### 功能 1：任务列表与状态

- 优先级：主要
- 使用场景：用户在装机过程中查看任务清单，新增、复制、剪切、粘贴、重命名、删除任务，并将任务标记为待处理、进行中或已完成。
- 数据文件：EXE 同级 `data\\tasks.json`；`data` 目录不存在时自动创建。若 EXE 同级目录不可写，进入只读运行模式，不偷偷改写到其他路径。
- 验收标准：能够新增、查看、编辑和删除任务，显示状态与优先级，任务数据重启后仍保留；删除任务前二次确认；工具栏操作只作用于任务数据，不创建、移动或删除真实资源库文件。

### 功能 2：资源库浏览与只读预览

- 优先级：主要
- 使用场景：用户在中部列表查看资源库文件夹和说明文件，选择 TXT/MD 文件后在右侧预览内容。
- 资源库根目录解析规则：
  1. 优先读取 EXE 同目录 `config.json` 的 `resourceLibraryPath`，按相对软件根目录解析；
  2. 若配置不存在或路径无效，按名称查找 EXE 同级的「资源库」文件夹；
  3. 仍未找到时，启动弹出目录选择框，由用户选定资源库目录后写回 `config.json` 的相对路径。
- 地址栏：固定显示资源库根路径（相对软件根目录），只读，不可通过地址栏导航到其他位置。
- 验收标准：能够进入/返回文件夹，显示名称、修改时间、类型和大小，并预览 TXT/MD 文本；资源库根目录缺失时按规定提示并可选择目录；应用不覆盖、移动、删除或批量改写资源库源文件。

### 功能 3：便携启动与受控执行

- 优先级：主要
- 使用场景：用户将发布目录复制到新装 Windows 11 x64 电脑后，直接启动 `TaskDesk.exe`；需要运行资源库中的工具时，通过双击发起受控启动。
- 验收标准：self-contained single-file EXE 无需预装 .NET 即可启动；双击 `.exe`、`.bat`、`.cmd`、`.ps1`、`.lnk` 前显示路径、类型和风险提示，确认后以普通用户权限启动，不自动提权；应用不修改被启动文件。

## 三、明确不做 ＋ 技术约束

- 明确不做：
  - 不写注册表、不安装服务、不创建计划任务、不修改网络或电源设置；
  - 不请求管理员权限、不自动提权；
  - 不修改资源库源文件，不在应用内覆盖、移动、删除或批量改写资源库文件；
  - 不登录、不上传数据、不依赖网络服务；
  - 不生成 zip；
  - 不打包安装器；
  - 不在应用内实现资源库文件编辑；需要修改时调用 Windows 文件资源管理器打开目录。
- 技术约束：C# WPF + .NET 8；Windows 11 x64；self-contained single-file 发布；应用启动时读取 Windows 系统主题，系统深色使用深色主题、系统浅色使用浅色主题，首版不做运行时切换动画；其余界面按图片采用结构优先布局（顶部路径栏/工具栏、左侧任务区、中部列表、右侧预览区）。

## 四、怎么交

- 源代码、测试代码、README、需求与交接文档；
- Windows 11 x64 self-contained single-file 可运行产物；
- 发布目录为 EXE 同级 `publish\\`，采用覆盖式输出；
- 版本号从 `v1.0.0` 起，版本信息集中在 `VERSION.md`；
- 初始化并启用 Git，提交信息遵循项目中文提交规则，`push` 必须单独授权；
- 不生成 zip，不提供安装器。

## 五、怎么验收

- 在 Windows 11 x64 环境直接启动 `publish\\TaskDesk.exe`，无需预装 .NET；
- 软件根目录为 EXE 所在目录，资源库按 `config.json` 相对路径、同级「资源库」目录、目录选择框的顺序解析；
- 地址栏固定显示资源库根路径（相对软件根目录），不可导航；
- `data\\tasks.json` 能自动创建并保存任务；目录不可写时进入只读运行模式且不回退写入其他目录；
- 任务增删改、状态切换、复制/剪切/粘贴、重命名、删除确认和重启持久化有效；
- 资源库浏览和 TXT/MD 预览有效，资源库源文件内容和路径不被修改；
- 受控执行按确认框和普通权限规则工作；
- 顶部、左侧、中部、右侧布局符合图片的结构优先标准，并按 Windows 系统主题启动为深色或浅色；
- 不写系统配置、不联网、不生成 zip、不打包安装器；
- 相关单元测试、集成测试和发布启动验证通过。

## 边界与授权

- 允许修改：本项目源码、测试、构建配置、需求文档、交接文档、项目内样例数据和 `publish\\` 发布输出；可初始化 Git 并按规则提交。
- 禁止：系统注册表、服务、计划任务、网络/电源配置；资源库源文件；外部上传、联网服务、管理员权限操作；zip 和安装器生成。
- 需再次确认：真实资源库程序执行、发布物替换到项目外目标、删除/覆盖/迁移项目文件、Git push，以及任何超出上述范围的外部写入。

## 提问状态

- 当前章节：提问结束
- 未决问题：无
