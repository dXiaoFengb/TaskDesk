# 对话记录

> 原始分支名：当前目录不是 Git 仓库，使用目录名“任务管理器”。

## 2026-09-23

- 时间：2026-09-23（具体时刻以本轮会话记录为准）
- 目标：阅读项目规则文件并确认执行边界。
- 改动：读取 `AGENTS.md`、`TASK.md`、`CONSTRAINTS.md`、`handoff.md`、`README.md`、`VERSION.md`；更新 `handoff.md` 和本记录文件，未修改代码、需求内容或约束红线。
- 验证结果：文档已读；Git 状态检查显示当前目录不是 Git 仓库；未运行测试、构建或发布流程。
- 未完成项：产品愿景、五节需求、边界与授权、项目专用约束及 Git 是否启用均待用户确认。

## 2026-09-23（资源库理解）

- 时间：2026-09-23
- 目标：确认暂不启用 Git，并开始理解 `资源库` 文件内容。
- 改动：记录 Git 未启用决定；仅执行资源库目录与文件清单读取，未修改资源库。
- 验证结果：已确认资源库包含 `A.Windows系统`、`B.Windows系统优化`、`C.工具库` 三个主区，以及根目录说明和三份整理记录；完整内容理解仍在本轮继续核对。
- 未完成项：需要继续读取根说明、A/B/C 整理记录及各分类说明文件，形成已确认的资源库结构摘要。

## 2026-09-23（资源库第一轮核对完成）

- 时间：2026-09-23
- 目标：理解资源库结构、主要文件用途与使用边界。
- 改动：只读读取资源库根说明、A/B/C 整理记录、区级说明和主要工具使用说明；未执行、复制、移动、删除或修改资源库文件。
- 验证结果：实时盘点共 6313 个文件：A 区 233 个约 30.66 GB，B 区 2267 个约 11.71 GB，C 区 3809 个约 12.04 GB；确认资源库的装机前/装机后/日常工具三段式定位。
- 未完成项：二进制、图片、压缩包及大型随包文档未逐字阅读；如需某个具体工具的执行判断，需再针对其说明、脚本原文和版本信息核验。

## 2026-09-23（2.进阶优化权限与构成确认）

- 时间：2026-09-23
- 目标：确认 `B.Windows系统优化\第二部分\2.进阶优化` 的修改授权与文件分类规则。
- 授权：用户明确授权可修改该目录；本轮仅只读理解，未执行任何脚本、注册表或程序。
- 已确认：目录共 28 个文件；11 个文件名以 `A.` 开头，按用户规则归为主要执行项；其余 17 个为可选项或说明文件。
- 重要细节：`4.网络优化\A.TCP加速.txt` 属于 A 组命名，但内容是 `A.TCP加速.bat` 的命令备份，不是执行文件；`5.高精度计时器禁用(英特尔禁止)` 明确标注 Intel 禁止，包含 BCD 引导配置修改；`6.Win32PrioritySeparation` 和 `7.屏幕缓冲区` 各有多个互斥方案，不能并行全选。
- 未完成项：尚未提出具体修改目标；下一步需按用户指定的改动逐项处理并验证。

## 2026-09-23（写入 2.进阶优化归类规矩）

- 时间：2026-09-23
- 目标：确认 A 执行项的特别含义与安全边界，并在目录根部写入长期归类规则。
- 改动：新增 `资源库\B.Windows系统优化\第二部分\2.进阶优化\00.进阶优化归类与执行规矩.txt`。
- 规则要点：A 表示主执行候选，不表示绝对安全或自动执行许可；新增文件须先读内容、确认作用/条件/回滚方式，再决定子目录和是否加一次 `A.` 前缀；BCD、驱动、服务、远程执行、未知二进制、数据删除等默认不得标 A。
- 验证结果：已回读新文件并确认写入成功；未执行任何优化文件。
- 未完成项：等待用户提供后续文件内容，按该规矩进行归类和 A 标记判断。

## 2026-09-23（服务脚本逐文件说明）

- 时间：2026-09-23
- 目标：解释 `01-恢复无用服务.bat` 与 `01-禁用无用服务.bat` 的实际作用。
- 改动：无；只读解析当前脚本内容，未执行或修改工具包。
- 已确认：两个脚本都会检查管理员权限并写入 HKLM 服务 `Start` 值；禁用脚本支持“禁用打印服务/保留打印服务”两种模式，恢复脚本把 Spooler 设为自动、其余目标服务设为手动。
- 风险结论：这是系统级服务配置修改，恢复脚本按固定默认值写回，不保存用户原有自定义状态；两者均不应按当前 A 规则标记为 A。

## 2026-09-23（2026 调机器工具包理解确认）

- 时间：2026-09-23
- 目标：理解工作区根目录 `2026调机器工具包` 的构成；用户明确要求不修改。
- 改动：无；仅读取目录结构、`使用前必读.txt`、批处理脚本和注册表文件的静态内容。
- 验证结果：工具包共 98 个文件，按 01～10 十个功能阶段组织，另有独立“计时器分辨率”目录及根部工具；确认包含成对的优化/恢复文件、互斥方案和维护回滚入口。
- 安全确认：未执行任何 `.bat`、`.reg`、`.exe`、`.msi` 或其他程序；工具包内存在服务、BCD/HPET、网络、显卡、输入、休眠、缓存删除和开机自启等系统级修改，不能视为无风险。
- 未完成项：如后续需要使用某个项目，需按具体文件逐项做适用性、回滚和实际效果核验。

## 2026-09-23（资源库关闭服务脚本说明）

- 时间：2026-09-23
- 目标：分析 `资源库\B.Windows系统优化\第二部分\1.服务\关闭服务.bat`。
- 改动：无；只读读取脚本及同目录 `设置详情.txt`，未执行或修改资源库。
- 已确认：脚本以管理员权限运行，使用 `sc stop` 立即停止并使用 `sc config ... start= disabled` 禁用 DiagTrack、Dps、RemoteRegistry、SysMain、WerSvc、WSearch、MapsBroker、GpuEnergyDrv 共 8 项服务。
- 风险结论：会影响诊断、远程注册表、预读取、错误报告、搜索索引、离线地图和 GPU 能源管理；脚本吞掉错误并无恢复脚本，不符合 A 标记条件。
- 待核实：同目录 `设置详情.txt` 只列出前 6 项服务，未列 MapsBroker 与 GpuEnergyDrv，说明文件可能过时或不完整。

## 2026-09-23（资源库恢复服务脚本说明）

- 时间：2026-09-23
- 目标：分析 `资源库\B.Windows系统优化\第二部分\1.服务\恢复服务.bat`，并与关闭脚本对照。
- 改动：无；只读读取脚本，未执行或修改资源库。
- 已确认：脚本覆盖 DiagTrack、Dps、RemoteRegistry、SysMain、WerSvc、WSearch、MapsBroker、GpuEnergyDrv 8 项服务；分别写入 auto、demand 或 delayed-auto，并随后逐项执行 `sc start`。
- 风险结论：会立即启动包括 RemoteRegistry 在内的服务；固定值不等于用户原始状态，错误输出被隐藏，因此只能视为预设恢复脚本，不是精确回滚，也不应标记为 A。

## 2026-09-23（修复恢复服务脚本）

- 时间：2026-09-23
- 目标：修复 `资源库\B.Windows系统优化\第二部分\1.服务\恢复服务.bat` 的恢复行为。
- 改动：取消对 8 项服务的立即 `sc start`；将 `MapsBroker` 从延迟自动调整为手动；增加每项配置失败计数、整体结果提示和重启提示；将“默认启动类型”措辞改为“预设启动类型”。
- 验证结果：已回读脚本并确认不再包含 `sc start`，8 项均使用 `sc.exe config`；未运行脚本，未进行运行时验证。
- 未完成项：预设启动类型是否完全符合目标 Windows 版本的原厂默认值，仍需在明确系统版本和实际服务配置后核验。

## 2026-09-23（修复关闭服务脚本）

- 时间：2026-09-23
- 目标：修复 `资源库\B.Windows系统优化\第二部分\1.服务\关闭服务.bat`。
- 改动：保留原 8 项服务范围；改用 `sc.exe`；先查询服务是否处于 RUNNING，仅对运行中的服务请求停止，再写入 `start= disabled`；增加启动配置失败和停止失败统计；补充与恢复脚本的回滚提示。
- 验证结果：已静态回读，确认包含 `sc.exe query/stop/config`，不包含 `sc.exe start`，条件语法已修正；未运行脚本，未进行运行时服务验证。
- 未完成项：服务默认值与具体机器兼容性仍需在目标 Windows 版本上单独核验。

## 2026-09-23（服务注册表本机快照与导入恢复）

- 时间：2026-09-23
- 目标：在关闭服务前按电脑分别保存服务注册表，并让恢复脚本直接导入对应快照。
- 改动：`关闭服务.bat` 新增 `reg.exe export` 快照流程，目录为 `服务配置备份\<计算机名>\<MachineGuid>\`，保存 8 个服务注册表键和备份信息；已有匹配快照时不覆盖，缺失/不匹配时拒绝禁用。`恢复服务.bat` 改为校验本机身份后逐项 `reg.exe import`，不再写死启动类型或立即启动服务。
- 验证结果：静态确认关闭脚本包含导出与身份校验，恢复脚本包含导入与身份校验且不含 `sc.exe config/start`；备份目录尚未生成，因为未运行关闭脚本。
- 未完成项：未进行真实服务关闭、注册表导出/导入和重启验证；首次使用前需人工确认快照目录和 8 个 `.reg` 文件均成功生成。

## 2026-09-23（关闭服务脚本闪退待定位）

- 时间：2026-09-23
- 目标：定位用户反馈的 `关闭服务.bat` 双击窗口一闪而过。
- 改动：无；只读检查脚本入口、退出点及备份目录状态。
- 已确认：脚本各正常/权限失败/快照失败路径均包含 `pause`；工作区尚未生成 `服务配置备份` 目录，因此尚无运行证据证明已进入导出阶段。
- 未完成项：需要确认具体启动方式和实际错误输出后，才能判断是权限、命令解释、系统环境还是文件关联问题。

## 2026-09-23（服务脚本运行结果只读验收）

- 时间：2026-09-23
- 目标：验收用户运行后的服务快照、当前服务状态和恢复条件。
- 改动：无；仅读取本机 `MachineGuid`、快照目录、`.reg` 文件及 8 项服务状态。
- 已确认：快照目录按 `DESKTOP-UKRDNNF\61ce5d49-3d07-4ce1-b173-b40a0acbda96` 分层，身份匹配；7 项服务当前均为禁用/停止，`GpuEnergyDrv` 在本机不存在或无法查询。
- 异常：快照仅有 7 个 `.reg` 文件，缺少 `GpuEnergyDrv.reg`；7 个现有快照的 `Start` 值均为 4（禁用），因此不能证明它们保存的是关闭前的原始状态，直接恢复可能仍保持禁用。
- 结论：关闭动作对 7 项服务生效，但“完整快照 + 可可靠恢复”目前未通过验收；未运行恢复脚本。

## 2026-09-23（2026 调机器工具包首批 A 标记）

- 时间：2026-09-23
- 目标：按已确认的 A 规则，先标记工具包中相对安全、作用明确且可回退的项目，交用户审核。
- 改动：仅改名加 `A.` 前缀，共 7 个文件；未改文件内容、未移动目录、未执行任何程序。
- 已标记：电源计划显示选项/解锁、计时器类型检测、DNS 缓存清理、键盘输入延迟、关闭鼠标加速、创建系统还原点。
- 未标记：服务禁用、CPU/显卡策略、BCD/HPET、TCP/DNS 设置、IPv6、USB/网卡线程、互斥手感方案、缓存/日志删除、开机自启和第三方工具。
- 验证结果：已检查 7 个目标源文件存在、目标文件名不存在后完成改名，并列出全部 A 文件；未进行运行时验证。

## 2026-09-23（按关闭日志修复恢复服务脚本）

- 时间：2026-09-23
- 目标：依据用户提供的关闭日志，修复 `资源库\B.Windows系统优化\第二部分\1.服务\恢复服务.bat`。
- 改动：保留本机 `ComputerName + MachineGuid` 校验、`.reg` 直接导入和 `.missing` 跳过；新增成功导入/跳过/禁用快照警告统计；脚本退出码改为实际导入失败数；保留原 GBK 编码。
- 验证结果：已回读脚本确认 8 项服务循环、`GpuEnergyDrv.missing` 跳过分支、`Start=4` 检测、失败计数返回码均存在；已确认现有快照 7 个 `.reg` 的 `Start` 均为 4，运行恢复后仍会保持禁用；未运行恢复脚本，未导入注册表。
- 未完成项：现有快照本身不是可证明的“关闭前原始状态”，无法仅靠恢复脚本修复其内容；如需恢复到真正关闭前状态，需要重新生成正确的关闭前快照后再测试。

## 2026-09-23（对照 03-CPU 核心调度与资源库）

- 时间：2026-09-23
- 目标：确认 `2026调机器工具包\03-CPU核心调度` 的功能是否在资源库中存在类似项。
- 改动：无，仅只读读取 03 文件夹内 16 个 `.reg` 文件并检索资源库文件名、注册表键和值及说明文档。
- 已确认：01 关闭 C-states、02 关闭降频/解锁全核，与 `2.进阶优化\1.基础优化\禁用CpuPwrThrottling（仅限台式机）.reg` 高度重叠；03 系统节能策略与该合并文件部分重叠。04～08 的按游戏进程设置 `CpuPriorityClass/IoPriority` 未在资源库找到对应项；资源库的 `7.Win32PrioritySeparation` 仅是全局调度参数，属于相关但不等效。
- 未完成项：未执行任何 `.reg`，未判断这些设置在当前电脑上的实际性能收益或兼容性。

## 2026-09-23（对照 04-显卡优化与资源库）

- 时间：2026-09-23
- 目标：确认 `2026调机器工具包\04-显卡优化` 的功能是否在资源库中存在类似项。
- 改动：无，仅只读读取 6 个显卡优化/恢复 `.reg` 文件并检索资源库注册表键、文件名和说明。
- 已确认：显卡抢占禁用使用 `nvlddmkm` 与 `GraphicsDrivers\Scheduler` 的 `DisablePreemption/EnablePreemption` 等键；Miracast 使用 `PlatformSupportMiracast`；显卡线程优先级使用 `nvlddmkm\Parameters\ThreadPriority`。资源库未找到这些同键功能。
- 相关但不等效：`A.NVIDIA驱动调度优化.reg`、`A.MPO修复-禁用多平面覆盖-双屏不同刷新率导入.reg`、`A.关闭GameDVR与后台录制-Xbox后台录制关闭.reg`，以及 USB 键鼠线程优先级文件；它们分别针对驱动调度、显示合成、后台捕获和 USB 线程，不等于 04 文件夹的三组设置。
- 未完成项：未执行任何 `.reg`，未判断这些设置在当前显卡驱动上的实际效果或兼容性。

## 2026-09-23（评估 04-显卡优化的收益与风险）

- 时间：2026-09-23
- 目标：判断 `2026调机器工具包\04-显卡优化` 的预期优化收益是否大于弊端。
- 改动：无；只读查询显卡、驱动和目标注册表值，并查阅 Windows 官方 WDDM/GPU 抢占与 Miracast 文档。
- 已确认：本机为 Intel 核显 + NVIDIA RTX 5060 Laptop GPU 混合显卡笔记本；三组脚本目标键均未设置，`PlatformSupportMiracast=1`；`netsh wlan show drivers` 返回“系统中没有无线接口”。
- 结论：禁用 GPU 抢占属于微软列出的驱动调试开关，且 WDDM 机制依赖抢占；禁用 Miracast 对当前无无线接口状态没有可验证收益，只会关闭能力；NVIDIA 私有的抢占/线程优先级值没有找到公开受支持的收益与兼容性说明。整体预期收益不大于风险，不建议列为 A 或常规优化项。
- 未完成项：未执行任何 `.reg`，也未进行对照帧时间、功耗、稳定性或投屏测试。

## 2026-09-23（对照 05-计时器延迟与资源库）

- 时间：2026-09-23
- 目标：确认 `2026调机器工具包\05-计时器延迟` 的功能是否在资源库中存在类似项。
- 改动：无，仅只读读取 9 个脚本/注册表文件，并检索资源库的 BCD 命令、计时器注册表键、可执行工具和说明。
- 已确认：`04-Win11计时器分辨率生效.reg` 与资源库 `4.计时器分辨率\A.win11分辨率计时器修改生效.reg` 均写入 `GlobalTimerResolutionRequests=1`；资源库包含 `SetTimerResolution.exe`，但没有同样的开机自启脚本。工具包与资源库的 HPET/动态时钟 BCD 优化都涉及 `useplatformclock/useplatformtick/disabledynamictick`，但写法及恢复方式不一致。
- 未找到：`tscsyncpolicy enhanced` 的 Intel TSC 同步、基于 Stopwatch 频率的计时器类型检测、`MeasureSleep.exe` 验证工具。
- 未完成项：未执行任何 BCD、注册表或计时器分辨率工具，未判断当前机器上的实际延迟、功耗与稳定性影响。

## 2026-09-23（对照 06-网络优化与资源库）

- 时间：2026-09-23
- 目标：确认 `2026调机器工具包\06-网络优化` 的功能是否在资源库中存在类似项。
- 改动：无，仅只读读取 12 个网络脚本/注册表文件，检索资源库 TCP、DNS、网络节流、网卡线程和 IPv6 内容。
- 已确认：`01-TCP传输性能优化.bat` 与资源库 `5.网络优化\A.TCP加速.bat` 直接对应；`02-关闭网络节流.reg` 与资源库 `A.关闭网络节流与后台延迟.reg` 写入相同的 `NetworkThrottlingIndex/SystemResponsiveness`。资源库的 `A.清除DNS缓存（ping值优化）.bat` 与工具包的 DNS 清理/设置方向相关，但只清缓存；`dns.txt` 只有 DNS 地址清单。
- 未找到：TCP/IP `ServiceProvider` 优先级重排、`NDIS\Parameters\ThreadPriority` 网卡线程优先级、批量关闭/恢复网卡 IPv6 绑定的同功能文件。
- 恢复差异：工具包的 TCP 与网络节流恢复文件在资源库没有逐项同名副本；网络节流恢复还原为删除 `NetworkThrottlingIndex`、`SystemResponsiveness=20`。
- 未完成项：未执行任何网络命令、DNS 修改、注册表导入或网卡绑定变更。

## 2026-09-23（对照 08-手感优先级与资源库）

- 时间：2026-09-23
- 目标：确认 `2026调机器工具包\08-手感优先级` 的功能是否在资源库中存在类似项。
- 改动：无，仅只读读取 11 个注册表文件并检索资源库的 Win32 调度和屏幕缓冲区设置。
- 已确认：工具包的 `Win32PrioritySeparation` 22、40、42 三档分别在资源库 `2.进阶优化\9.Win32游戏优先级` 中存在；资源库版本额外写入 `ConvertibleSlateMode=0`。工具包 26 档在资源库未找到。工具包的 1080P、2K、4K、6K、3840x1440 五种屏幕缓冲区值在资源库 `2.进阶优化\10.屏幕缓冲区` 中全部存在且数值一致。
- 未找到：工具包 `05-恢复Win32默认.reg` 与 `11-恢复屏幕缓冲区默认.reg` 的同功能恢复文件。
- 未完成项：未导入任何注册表，也未验证不同档位对实际手感、帧时间或显示稳定性的影响。

## 2026-09-23（对照 09-系统响应与资源库）

- 时间：2026-09-23
- 目标：确认 `2026调机器工具包\09-系统响应` 的功能是否在资源库中存在类似项。
- 改动：无，仅只读读取 10 个注册表文件并检索资源库对应注册表键和值。
- 已确认：桌面响应的 `AutoEndTasks/HungAppTimeout/WaitToKillAppTimeout/LowLevelHooksTimeout/MenuShowDelay/WaitToKillServiceTimeout`、错误报告 `Disabled`、后台应用 `GlobalUserDisabled`、休眠 `HibernateEnabled`、定时器合并 `CoalescingTimerInterval` 在资源库均未找到同键实现。
- 相关但不等效：资源库服务脚本涉及 `WerSvc`，可影响错误报告服务；`Win32PrioritySeparation` 与 `SystemResponsiveness`涉及前后台资源分配；计时器分辨率/BCD文件涉及计时器机制，但都不能替代 09 文件夹的设置。
- 未完成项：未导入任何注册表，未关闭服务、后台应用、休眠或定时器合并。

## 2026-09-23（对照计时器分辨率文件夹与资源库）

- 时间：2026-09-23
- 目标：确认 `2026调机器工具包\计时器分辨率` 的功能是否在资源库中存在类似项。
- 改动：无，仅只读读取文件清单、文本说明并计算三个核心文件的 SHA-256。
- 已确认：`MeasureSleep.exe`、`SetTimerResolution.exe`、`win11分辨率计时器修改生效.reg` 与资源库 `2.进阶优化\4.计时器分辨率` 对应文件 SHA-256 一致，属于同一文件；资源库还提供开机自启脚本、验证脚本、计时器类型检测及 HPET/TSC 调整/恢复脚本。
- 未找到：工具包的 `计时器分辨率win10解决方案.msi`、`Autoruns.exe` 在资源库没有同名同功能文件；工具包的启动说明文本与资源库 `Windows+r输入.txt` 仅部分相关，资源库文本只有启动目录路径。
- 未完成项：未运行 MSI、EXE、脚本或注册表文件。

## 2026-09-23（识别 Autoruns.exe）

- 时间：2026-09-23
- 目标：说明 `2026调机器工具包\计时器分辨率\Autoruns.exe` 的用途与来源。
- 改动：无；只读读取版本信息、数字签名和 SHA-256，并查阅 Microsoft Sysinternals 官方说明。
- 已确认：该文件是 Sysinternals Autoruns v14.11（Autostart program viewer），数字签名有效，签署者为 Microsoft Corporation；用于枚举 Windows 的登录项、启动文件夹、服务、计划任务、驱动及其他自动启动位置，可禁用或删除启动项。
- 未完成项：未运行程序，未扫描或修改本机任何启动项。

## 2026-09-23（更新 2.进阶优化使用前必读）

- 时间：2026-09-23
- 目标：根据当前更新后的 `2.进阶优化` 内容，更新 `使用前必读.txt`。
- 改动：重写该文件的目录说明、A 标记解释、执行前置条件、风险分类、操作流程和常见问题；以当前 1～10 实际目录为准，移除旧版不存在的“09-系统响应”流程；纠正所有项目都有恢复文件及可放心批量执行等不准确表述。
- 验证结果：已按 GBK 编码回读，确认当前版、A 规矩、BoosterX 风险、逐项执行和恢复边界等关键内容存在；未执行任何 EXE、MSI、BAT、REG 或系统修改项。
- 未完成项：文档说明不会替代各具体文件的逐项适用性核验和用户执行授权。

## 2026-09-23（TaskDesk 文档与项目骨架）

- 时间：2026-09-23
- 目标：按已确认的 TaskDesk 需求补齐四份项目文档，初始化 Git，并创建 WPF/.NET 8 项目骨架。
- 改动：重写 `TASK.md`、`CONSTRAINTS.md`、`README.md`、`handoff.md`；更新 `VERSION.md` 为 `v1.0.0`；初始化当前目录 Git；新增 `.gitignore`、`global.json`、`src/TaskDesk` WPF 骨架、`tests/TaskDesk.Tests` 测试骨架、`data/.gitkeep` 与 `publish/.gitkeep`；将受保护的 `资源库/` 加入 `.gitignore`。
- 验证结果：四份文档关键规则已回读；项目 XML/XAML 文件解析通过；Git 状态显示尚无提交且资源库被忽略；本机未安装 .NET SDK，仅有运行时，因此未运行 restore/build/test/publish。
- 未完成项：等待安装 .NET 8 SDK 后进行构建、测试、发布与 Windows 11 x64 手工验证；已提交 Git `2fc11ee`，未执行 push，未生成 zip 或安装器。
## 2026-09-23（TaskDesk 核心数据层）

- 时间：2026-09-23
- 目标：解决 .NET SDK 阻塞并实现 TaskDesk 任务数据与资源库定位核心层。
- 改动：在项目目录准备隔离 .NET SDK 8.0.425；新增任务模型/状态、`data\\tasks.json` 持久化、`config.json` 读写、资源库三段定位与基础测试；修正测试项目目标框架和 `.gitignore`。
- 验证结果：主项目 Release 构建通过；测试项目 3 项全部通过；未修改资源库源文件，未生成 zip 或安装器。
- 未完成项：尚未接入主窗口交互，尚未发布 self-contained EXE，尚未完成 Windows 11 UI/DPI 手工验证。
## 2026-09-23（TaskDesk 窗口接入）

- 时间：2026-09-23
- 目标：将任务数据层与资源库只读浏览接入三栏 WPF 窗口，并实现系统主题读取。
- 改动：新增资源条目/浏览器、命令、主题服务和主窗口 ViewModel；实现资源库选择、固定根地址栏、目录进入/返回、TXT/MD 预览及任务条目的新建、复制、剪切、粘贴、重命名、删除和状态更新。
- 验证结果：Release 构建通过（0 警告、0 错误）；3 项核心测试全部通过；未修改资源库源文件。
- 未完成项：尚未运行 self-contained 发布，尚未完成 Windows 11 UI/DPI 手工验证，资源库工具的确认后普通权限启动尚未接入 UI。
## 2026-09-23（TaskDesk v1.0.5 候选构建）

- 时间：2026-09-23
- 目标：生成 self-contained 单文件候选，并修正 EXE 版本元数据与版本台账一致。
- 改动：将程序集版本绑定到 `1.0.5.0`，生成 `publish\\TaskDesk.exe`；未生成 zip 或安装器。
- 验证结果：Release 构建通过（0 警告、0 错误）；3 项核心测试全部通过；候选 EXE 大小 153,447,654 字节，FileVersion `1.0.5.0`，SHA-256 `8BA32D3752E55A33C4F5100ED37B08A7828B03A14080369B315EB3E22288F162`。
- 未完成项：尚未完成 Windows 11 UI/DPI 手工验证，尚未接入资源库工具确认后普通权限启动。
## 2026-09-23（TaskDesk v1.0.6 受控执行）

- 时间：2026-09-23
- 目标：补齐资源库工具双击确认和普通权限启动规则。
- 改动：新增 `ResourceExecutionService`，仅允许 `.exe/.bat/.cmd/.ps1/.lnk`；双击时弹确认框，使用 ShellExecute 普通用户权限启动，不修改资源文件；新增扩展名和缺失文件测试。
- 验证结果：Release 构建通过（0 警告、0 错误）；4 项核心测试全部通过；候选 EXE 大小 153,451,750 字节，FileVersion `1.0.6.0`，SHA-256 `33485BA703033E1326995D6494EF3B8C9D5F61B758C8D266C4A0FE2713F623D3`。
- 未完成项：尚未完成 Windows 11 UI/DPI 手工验证，未实际启动资源库中的外部程序。
## 2026-09-23（TaskDesk v1.0.7 候选交接）

- 时间：2026-09-23
- 目标：让最新候选 EXE、程序集版本、版本台账和交接状态一致。
- 改动：版本递增到 `v1.0.7`，候选 EXE FileVersion 为 `1.0.7.0`；未生成 zip 或安装器。
- 验证结果：Release 构建通过（0 警告、0 错误）；4 项核心测试全部通过；候选 EXE 大小 153,451,750 字节，SHA-256 `BA3723D42F2EA53A1588F35E6D0971599C26C0DD6AD90291075CDD7C58A5B5E1`。
- 未完成项：等待 Windows 11 UI/DPI 手工验证和真实外部资源启动验证。
## 2026-09-24（TaskDesk 启动崩溃修复与根目录入口）

- 时间：2026-09-24
- 目标：将启动入口移到软件根目录，并查明/修复 TaskDesk 启动崩溃。
- 证据：Windows `.NET Runtime` 事件 1026 明确记录 `RootAddress` 只读属性被 `TextBox.Text` 默认 TwoWay/OneWayToSource 绑定，抛出 `InvalidOperationException`；对应 Application Error 1000 异常代码为 `0xe0434352`，故障路径为 `publish\\TaskDesk.exe`。
- 改动：固定地址栏绑定为 `Mode=OneWay`；self-contained 发布输出改为软件根目录 `TaskDesk.exe`；保留 `publish\\` 既有候选作为回退副本；启用原生库自解压；同步需求、约束和 README。
- 验证结果：Release 构建通过（0 警告、0 错误）；4 项核心测试全部通过；根目录 TaskDesk.exe 启动存活 5 秒后正常结束测试进程，近五分钟无新增 TaskDesk 崩溃事件；FileVersion `1.0.8.0`，大小 161,668,557 字节，SHA-256 `013E7899E77249B4E25043A6C9F56B9A84F50190BF692EF493A6A60AD1E0F8DA`。
- 未完成项：尚未完成 Windows 11 UI/DPI 手工验证，未实际启动资源库中的外部程序。