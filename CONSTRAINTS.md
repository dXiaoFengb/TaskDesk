# TaskDesk 项目特有约束

> 通用规则见 `AGENTS.md`；项目需求与授权见 `TASK.md`。本文件是本项目红线与加严项的唯一来源。

- 项目标识：中文名「装机任务管理器」；英文代号 `TaskDesk`；EXE 为 `TaskDesk.exe`；根命名空间为 `TaskDesk`。
- 软件根目录：EXE 所在目录。
- 资源库根目录：优先读取 EXE 同目录 `config.json` 的 `resourceLibraryPath`（相对软件根目录）；无效或不存在时按名称查找 EXE 同级「资源库」文件夹；仍未找到时弹出目录选择框，用户选定后写回 `config.json`。地址栏固定显示资源库根路径（相对软件根目录），只读且不可导航。
- 数据路径：EXE 同级 `data\\tasks.json`；`data` 不存在时允许自动创建；EXE 同级目录不可写时进入只读运行模式，不偷偷改写到其他路径。
- 只读路径：资源库根目录及其全部内容 — 允许读取、列出、预览和按确认规则启动文件；不覆盖、不移动、不删除、不改名、不批量改写、不复制到工作区或外部。如需修改，必须由用户通过系统文件资源管理器操作，并不由 TaskDesk 代办。
- 发布目标：软件根目录 `TaskDesk.exe`；仅在相关测试通过且用户明确授权后替换；替换后核对版本与 SHA-256。发布采用覆盖式输出。`publish\\` 中的既有候选仅作回退副本，不作为启动入口。
- 发布限制：不生成 zip；不打包安装器；只提供 Windows 11 x64 self-contained single-file 发布物。
- 外部副作用：不写注册表、不安装服务、不创建计划任务、不修改网络或电源设置；不发送消息、不上传数据、不依赖联网服务、不跑生产任务、不写外部系统，除非用户明确授权并记录。
- 权限限制：不请求管理员权限、不自动提权；资源库中的 `.exe`、`.bat`、`.cmd`、`.ps1`、`.lnk` 仅在用户确认后以普通用户权限启动。
- Git 加严：本项目启用 Git 提交；当前执行阶段允许初始化仓库和按规则提交。禁止自动 `push`；`push` 必须单独获得用户授权。提交信息使用中文并遵循 `AGENTS.md` 的版本格式与检查要求。
- 版本规则：从 `v1.0.0` 起，版本信息集中在 `VERSION.md`；每次提交前按规则递增版本并更新版本台账。发布状态必须有测试、实际产物替换、版本和 SHA-256 证据。
- 授权记录：每项超出本文件默认范围的操作记录操作、目标、范围、期限和确认来源。
- 只读清单变更：须用户明确确认并记录，不得由执行者自行移除或放宽。
