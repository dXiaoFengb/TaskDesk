# 项目协作协议

除文末底线外，本文件以及同目录的其他 md 文档都只是起点，不是定稿：请按项目实际情况增删章节、改写措辞，把它们变成该项目的专用文档。文件名也可调整，改后保持互相引用一致。

默认优先级（高到低）：文末底线 > 用户当场明确指令 > `CONSTRAINTS.md` > `TASK.md` > `AGENTS.md`。

冲突时取更严格、更具体者；`TASK.md` 的禁止项与需确认项不得被通用 `AGENTS.md` 放宽。用户授权必须明确到操作、目标、范围、期限；长期红线授权记入 `CONSTRAINTS.md`，本轮授权与未授权事项记入 `handoff.md`，本项目允许修改／禁止／需再次确认的范围记入 `TASK.md` 的「边界与授权」，三者分工不得互相替代；模糊、推测、默认同意不算授权。这条链和下列规则都可由项目改写（文末底线除外）；未规定的事项由执行者自行判断，除破坏性操作须先确认外不必请示。

## 工作方式

- 中文沟通；结论以已验证事实为准，无法验证时写 `[待核实]`。
- 开始或续接任务时先读本目录的 `AGENTS.md`、`TASK.md`、`CONSTRAINTS.md`、`handoff.md`；`README.md` 作为项目总览和启动说明。
- 用一句话复述目标与边界。
- 本目录的 md 若还是通用模板，先按项目实际信息改写、补全（项目档案、红线、任务目标、版本规则），再开始任务；无法确定的部分保持占位并问用户。
- 报错或异常先查证据再改，不凭猜测下结论。
- 不泄露凭证与个人信息；不伪造测试结果、来源、版本号或发布状态。

## 需求澄清流程

- 前置输入是产品愿景：`TASK.md` 的「产品愿景」为空时，先一次只问「为什么做、给谁用、得到什么价值」并确认，再进入第一节；这一步同样采用“问题 + 2–4 个基于已知上下文的候选方向 + 我自己填”，没有可靠上下文时不得凭空生成选项，只保留「我自己填」。
- 若任务包含新项目立项或需求收集，结合已确认的产品愿景，严格按 `TASK.md` 的五节骨架逐节提问：项目目的、功能任务、明确不做＋技术约束、怎么交、怎么验收。
- 一次只问一节，不跳步、不合并、不提前写代码；若已有需求文档模板，以其标题与顺序为准。第四节（怎么交）通常由需求方给定，直接确认即可，不必反问。
- 每次提问固定包含“问题 + 2–4 个结合产品愿景生成的选项 + 我自己填”，不能凭空编造选项或照抄其他项目。
- 一次只问一件事，语气简洁，不长篇解释。
- 用户给出明确答案后记录并进入下一节；回答“不清楚 / 不知道 / 随便”时先用更具体的问题追问一次，仍不明确才推荐最简单的默认方案，并说明后续可修改。
- 五节问完后，再确认 `TASK.md` 的「边界与授权」一节（允许修改 / 禁止 / 需再次确认），不得跳过。
- 五节与「边界与授权」全部确认后明确说“提问结束”，再按 `TASK.md` 的输出格式生成正式需求文档；「边界与授权」是本项目范围（允许修改／禁止／需再次确认）的权威，但不得覆盖 `CONSTRAINTS.md` 的红线，结果同步到 `handoff.md` 的「本项目授权范围」，不进需求文档正文。需求未收敛前不得把推测写成已确认内容。
- 收敛判据：用户不看对话记录也能自己讲一遍「要做什么、不做什么」，即可结束提问。

## 需求变更

- 需求确认后如需变更，一律由用户提出，执行者不得自行改动已确认内容。
- 变更先记入 `TASK.md` 对应章节：保留原内容、就地标注变更日期与原因，并按需把变更前版本另存到项目约定的历史目录；再同步 `handoff.md` 的「改动」与「下一步」，不得静默覆盖已确认内容。
- 影响项目目的、功能主次、明确不做、技术约束、交付方式或验收标准的变更，须重新向用户确认；涉及 `CONSTRAINTS.md` 红线或既有授权的，按该文件的规定另行授权。
- 变更后按需重新生成需求文档：旧版本另存到项目约定的历史目录（默认 `需求文档/历史/`，不存在时创建），文件名带日期，不直接覆盖。

## 文件与代码

- 只改完成任务所必需的文件；不做无关格式化、重排或重命名。
- 删除、覆盖、迁移、批量改写、权限调整或对外写入前，先说明精确目标与影响范围；未经授权不执行。
- 测试用夹具、临时目录或只读检查，不触碰源数据，不跑生产任务。
- 改完跑最小必要验证；未跑或未过不得宣称成功。

## 版本与 Git

- 版本号与构建标记集中在一处维护（默认 `VERSION.md`），其他文件只引用。
- 首次使用本模板（新项目第一次对话）时，先通知用户并询问是否启用 Git 提交，答复记入项目级唯一位置——`CONSTRAINTS.md` 的「Git 加严」条（`README.md` 项目档案只引用，不得另存副本），不得只记在 `handoff.md`（该文件按轮次覆盖，记不住项目级决定）；用户未答复或未启用前，不得执行任何 `commit`。
- 每次提交都对应一个版本号：提交前递增 `VERSION.md` 构建标记行的 `version` 与 `date`，并在其版本表末新增一行（状态记「草稿」）。递增口径以 `VERSION.md` 的版本规则为准：向后兼容的功能新增递增次版本号，修复／文档／构建修正递增修订号（两者合称小版本号）；不兼容变更、发布流程变化、对外接口或发布物结构变化递增主版本号（大版本号）。同一版本号只用于一次提交；`codename` 与 `chinese` 按发布线维护，不随每次提交变更。
- 发布完成的判据：测试通过 + 目标发布物已实际替换 + 版本与 SHA-256 已核对；候选产物不算已发布。
- 经用户明确授权后可自动 `git add <明确文件>` + `git commit`；未获授权前禁止自动 `commit`；禁止默认使用 `git add -A` / `git add .`。
- 自动提交时机（仅在已启用 Git 提交时）：每轮对话结束、更新完 `handoff.md` 与对话记录后提交一次；单个功能点完成时也可单独提交。粒度以「一次提交对应一件可说明的改动」为准，不得把无关改动混进同一次提交。未启用时，改为每轮结束列出待提交文件清单并等用户指示。
- `git commit` 提交信息一律使用中文；必须以版本号开头，格式为 `<版本号> <二字概括>:<详细说明>`，例如 `v0.1.0 修复:修正登录超时`；禁止以「更新」「修复完成」等介绍性文字开头。版本号取自 `VERSION.md` 构建标记行的 version 字段，须与本次提交前递增后的结果一致；二字概括写更改的大致意思，如 `功能`、`修复`、`文档`、`重构`、`测试`、`构建`、`杂项`；详细说明写清改了什么，可续写原因、影响与验证。禁止无意义信息，如 `update`、`fix`、`.`。
- 所有 Git 注释信息一律使用中文：提交信息、合并提交信息（`merge`）、标签说明（`git tag -m`）、PR / MR 的标题与描述。其中说明性文字必须是中文，不得整条改用英文；命令、代码标识符、路径、日志原文按原样保留，不强制翻译。本条规定为唯一出处，其他文件只引用。
- 提交前检查 `git status --short` 与 `git diff --cached`；不提交凭证、密钥、个人信息。
- `push`、重写历史、清理工作区须单独授权。

## 对话记录

- 每轮对话结束前写一份记录并保存，不必等用户要求；同一分支的记录追加到同一文件。
- 路径：`对话记录/<安全分支名>.md`。分支名中的 `/`、空格等替换为 `__`，并在文件内记录原始分支名；不是 Git 仓库或取不到分支名时，用目录名代替。
- `对话记录/` 目录不存在时由执行者自动创建；是否纳入版本控制由项目在 `CONSTRAINTS.md` 中确定，未规定时默认纳入。
- 每条记录写：时间、目标、改动（文件与原因）、验证结果、未完成项。
- 记录中不得写入凭证、密钥、个人信息。
- 记录是按对话累积的过程档案，不替代 `handoff.md` 的当前状态。

## 交付

- 说明改动、验证结果、未完成项与 Git 状态；有发布物时附版本与 SHA-256。
- 结束前把同样内容更新到 `handoff.md`，别让交接文档停在空模板。

## 底线（不可改写）

以上任何改写都不得放宽：不伪造结果、不泄露凭证、不触碰 `CONSTRAINTS.md` 标明的只读数据。

不得通过改写 `CONSTRAINTS.md` 或其他文档擅自移除、放宽只读数据、发布授权、外部副作用限制。变更只读清单须用户明确确认并记录。
