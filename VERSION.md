# 版本与发布记录

<!-- build: version=v1.0.7; codename=taskdesk; chinese=装机任务管理器; date=2026-09-23 -->

版本、代号、中文名、日期与构建标记集中在此文件维护；构建脚本读上面的标记行，其他文件只引用。

## 版本规则

- 主版本：不兼容变更、发布流程变化、对外接口或发布物结构变化时递增。
- 次版本：向后兼容的功能新增时递增。
- 修订号：修复、文档、构建修正时递增。
- 预发布：`vX.Y.Z-alpha.N`、`vX.Y.Z-beta.N`、`vX.Y.Z-rc.N`。
- 正式版：`vX.Y.Z`；候选版不得标记为已发布。
- 构建标记行是当前版本（版本、代号、中文名、日期）的唯一来源，其他文件只引用；下方版本表是版本台账，须与标记行保持一致。
- 版本变更须由用户明确授权，或按项目另行规定的发布流程执行。
- 按 `AGENTS.md` 的规定随每次提交递增版本号，视为本项目已定义的版本变更流程；递增口径见本节上文的主版本／次版本／修订号定义。
- 逐次递增时只改构建标记行的 `version` 与 `date`，并在下方版本表末新增一行（版本、代号、中文名、日期，状态记「草稿」）；`codename` 与 `chinese` 按发布线维护，同一发布线内保持不变。
- 仅经用户明确授权并完成发布核对后，版本状态才由「草稿」转为「候选」或「已发布」；「发布历史」只记录已发布的版本，与逐次提交无关。
- 发布状态流转：草稿 -> 候选 -> 已发布；回滚须记录原因与影响。

| 版本 | 代号 | 中文名 | 日期 | 状态 |
| --- | --- | --- | --- | --- |
| v1.0.1 | taskdesk | 装机任务管理器 | 2026-09-23 | 草稿 |
| v1.0.2 | taskdesk | 装机任务管理器 | 2026-09-23 | 草稿 |
| v1.0.3 | taskdesk | 装机任务管理器 | 2026-09-23 | 草稿 |
| v1.0.4 | taskdesk | 装机任务管理器 | 2026-09-23 | 草稿 |
| v1.0.5 | taskdesk | 装机任务管理器 | 2026-09-23 | 草稿 |
| v1.0.6 | taskdesk | 装机任务管理器 | 2026-09-23 | 草稿 |
| v1.0.7 | taskdesk | 装机任务管理器 | 2026-09-23 | 草稿 |

## 发布历史

只记录有证据的交付；证据缺失就写「发布证据缺失」，不依据目录名、文件时间或记忆补写哈希与测试数。

| 版本 | 日期 | 交付要点 | 测试 | 发布物与 SHA-256 |
| --- | --- | --- | --- | --- |
|  |  |  | 命令 + 环境 + 结果 + 证据位置 | 发布物对象 + SHA-256 |
